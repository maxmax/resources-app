import React from 'react';
import TabList, { TabListProps } from '@mui/lab/TabList';

const TabListComponent: React.FC<TabListProps> = (props) => {
  return <TabList {...props} />;
};

export default TabListComponent;
